import { renderWrapper } from '../../renderWithProviders';
import {{properCase name}} from './{{properCase name}}';

describe('{{properCase name}}', () => {
  it('renders and finds the components text content', () => {
    const { getByText } = renderWrapper(<{{properCase name}} />);

    expect(getByText('{{properCase name}} created via plop')).toBeInTheDocument();
  });
});
