import { useState } from 'react';

const use{{properCase hookName}} = () => {
  const [{{camelCase hookName}}, set{{properCase hookName}}] = useState<string>('');

  return { {{camelCase hookName}}, set{{properCase hookName}} };
};

export default use{{properCase hookName}};
